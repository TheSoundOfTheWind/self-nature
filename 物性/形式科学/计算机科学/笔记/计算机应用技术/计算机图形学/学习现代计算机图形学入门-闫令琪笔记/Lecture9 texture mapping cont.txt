纹理的应用
顶点的变换和处理
像素的变换和处理
顶点着色器
像素着色器

三角形的不同位置映射到纹理的不同位置

纹理不只是颜色，还有很多强大的应用

重心坐标，三角形的做插值
为什么要在三角形内部做插值
三角形内部有一个平滑的过度

插值什么内容
顶点 到 纹理的u v

重心坐标
(x, y) = 
插值，位置、纹理、坐标、颜色、法线、深度，材质

在投影下可能会发生变化

线性插值 lcrp(x, v0, v1) = v0 + x(v1 - v0)

小图贴在大图上
小图放大可以用双线性插值

Nearest

Bilinear

Bicubic

三种插值方法

好质量的东西往往带来更大的开销，然后在慢慢实现同样
的质量然后降低开销

纹理太大了
远处摩尔纹，近处锯齿 走样

像素覆盖纹理的区域是不一样的，
多个像素在同一个纹理中，或一个像素覆盖了多个纹理

超采样

频率越高采样要越密集才能获取足够完整的信息

不采样，取平均

点查询和范围查询问题

Mipmap 允许做范围查询
图像金字塔，多了三分之一的存储量
fast approx 

